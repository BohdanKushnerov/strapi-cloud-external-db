'use strict';

/**
 * section-hero-carousel controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::section-hero-carousel.section-hero-carousel');
