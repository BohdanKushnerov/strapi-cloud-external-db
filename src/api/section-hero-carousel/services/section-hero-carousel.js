'use strict';

/**
 * section-hero-carousel service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::section-hero-carousel.section-hero-carousel');
